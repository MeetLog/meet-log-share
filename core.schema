type User{
	id: ID
	name: String
	nfcIdm: String
	iconUrl: String
}

type Query{
	login(
		name: String
		encryptedPassword: String
		nfcIdm: String
		iconUrl: String
	): User
}
schema{
	query: Query
}