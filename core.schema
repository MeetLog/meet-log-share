type User{
	id: ID
	name: String
	password: String
	nfcIdm: String
	iconUrl: String
}

type Query{
	login(
		name: String
		encriptedPassword: String
		nfcIdm: String
		iconUrl: String
	): User
}
schema{
	query: Query
}