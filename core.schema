type User{
	id: ID
	name: String
	nfcIdm: String
	iconUrl: String
}

type Query{
	login(
		id: String
		password: String
	): User
}

type Mutation{
	register(
		name: String
		password: String
		nfcIdm: String
		iconUrl: String
	): ID
}
schema{
	query: Query,
	mutation: Mutation
}